spring:
  application:
    name: sensor-config
  cloud:
    config:
      enabled: false
#      uri: http://127.0.0.1:8888
    consul:
      host: ${CONSUL_ADDR:localhost}
      port: ${CONSUL_PORT:8500}